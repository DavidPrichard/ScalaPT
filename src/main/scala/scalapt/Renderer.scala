package scalapt


class Renderer {



  // CONCURRENCY
  // intersection/collision strategy
  // termination strategy
  // sampling strategy (halton, sobol, other prng, naive(one-bounce), correlated) etc
  // supersamples(???)
  // gamma/colorspace conversion
  // hdr
  //
  //
  // metropolis light transport
  // direct light sampling
  // adaptive sampling (all points that are pure black or pure white)
  // etc.
}
